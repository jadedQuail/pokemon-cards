{
    "name": "pokemon-cards",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install --prefix back-end && npm install --prefix front-end && npm run generate --prefix front-end",
        "start": "npm run --prefix back-end start"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^9.2.0"
    }
}
